{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:routes": "vitest run test/route-manifest.test.ts"
  },
  "dependencies": {
    "@talon/contracts": "workspace:*",
    "@talon/db": "workspace:*",
    "@talon/domain": "workspace:*",
    "awilix": "^12.0.5",
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
