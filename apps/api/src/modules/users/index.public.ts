// The module's published interface — the only legal import target from other
// modules. Everything not exported here is private.
export type { UsersService } from './service.js';
export { registerUsers } from './container.js';
